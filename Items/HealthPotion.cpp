//
// Created by Micha on 22/01/2025.
//

#include "HealthPotion.h"

#include <thread>

HealthPotion::HealthPotion() {
    m_healthRegeneration = 20;
}


 void HealthPotion::usePotion() {
//     int HealthBeforeBuff = player->getHealth();
//     player->setHealth(HealthBeforeBuff + m_healthRegeneration);
//     std::thread cancelHealthBonusThread([this, player]() {
//             cancelBonus(player);
//         });
//     cancelHealthBonusThread.detach();
 }
//
//
void HealthPotion::cancelBonus() {
//     std::this_thread::sleep_for(std::chrono::seconds(30));
//     player->setHealth(player->getHealth() - m_healthRegeneration);
 }
